"""跨模块公共常量与全局开关。

只放"多个业务模块都要用"的东西：账号类型、账号状态、联赛类型、IO 开关、DB 路径。
各业务模块自己的参数（列映射、权重、阈值等）就近放在各模块的 config.py 内。
"""

# ---------------------------------------------------------------------------
# 账号类型
# ---------------------------------------------------------------------------
ACCOUNT_TYPE_COMBAT = "combat"   # 战营账号（默认参加实战）
ACCOUNT_TYPE_NORMAL = "normal"   # 普通账号（报名时选择是否实战）

# ---------------------------------------------------------------------------
# 账号状态
# ---------------------------------------------------------------------------
STATUS_ACTIVE = "active"          # 活跃（本月已报名）
STATUS_MISSED = "missed"          # 漏报（1 个月未报名）
STATUS_MAYBE_LEFT = "maybe_left"  # 疑似离开（连续 N 月未报名）
STATUS_LEFT = "left"              # 已离开（人工确认）

# ---------------------------------------------------------------------------
# 联盟部落成员身份（由 coc_sync 维护，与上面的"报名维度状态"是两套独立体系）
#   member -> 当前在联盟旗下某部落内
#   left   -> 已退出联盟部落（coc-sync 对账发现不在成员列表且新部落非联盟部落）
# ---------------------------------------------------------------------------
MEMBERSHIP_MEMBER = "member"
MEMBERSHIP_LEFT = "left"

# ---------------------------------------------------------------------------
# 联赛类型
# ---------------------------------------------------------------------------
LEAGUE_COMBAT = "combat"  # 实战联赛
LEAGUE_SHELL = "shell"    # 壳子联赛

# ---------------------------------------------------------------------------
# IO 适配器开关：local / tencent
#   tencent -> 默认读写腾讯在线文档（需配置 TENCENT_DOC_ACCESS_TOKEN /
#              TENCENT_DOC_CLIENT_ID / TENCENT_DOC_OPEN_ID 等环境变量）。
#   单条命令可用 --to local/tencent 覆盖此全局默认。
# ---------------------------------------------------------------------------
IO_ADAPTER = "tencent"

# ---------------------------------------------------------------------------
# 数据库路径
# ---------------------------------------------------------------------------
DB_PATH = "data/league.db"
